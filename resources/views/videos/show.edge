@layout('layouts/main')
@set('title', 'Vídeo')

@section('body')
  <div class="flex flex-col my-6">
    <div class="flex justify-center items-center">
      <tr>
        <td>{{ video.title }}</td>
        <td>{{ video.description }}</td>
      </tr>
    </div>
  </div>
@endsection